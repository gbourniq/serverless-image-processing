serverless sample project

* AWS diagram for each lambda function (thumbnail + SNS/Slack notification + DynamoDB image metadata)
* manage secret with aws parameter store? code from django-on-aws 
* integrate pydantic + layer, makefile, existing CI/CD scripts, invoke local / sls test, and keep code simple
* review and simplify code base
* unit tests
* Lambda destination
* add README
* add x ray sdk
* try integrating codedeploy? + versions / aliases
* add dev/test/prod api stage and stage variables (see udemy course)
* add step function to orchestrate a few lambda functions - read step function use cases